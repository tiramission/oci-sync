[project]
name = "e2e"
version = "0.1.0"
description = "E2E test for oci-sync"
requires-python = ">=3.10"
dependencies = [
    "rich>=13.0.0",
]

[build-system]
requires = ["uv_build>=0.11.7,<0.12"]
build-backend = "uv_build"